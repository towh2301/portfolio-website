/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"images.unsplash.com",
			"themoviedb.org",
			"dlcorp.com.vn",
			"simplenote.com",
		],
	},
};

module.exports = nextConfig;
